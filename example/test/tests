/build/
 /example/
 /examples/
 /generate/
 /test/
 /vendor/Release/

 /generate/output
 /generate/**/*.json
 !/generate/input/*.json

 baton->req.data = baton;
   baton->done = false;

   while(!baton->done) {
     this_thread::sleep_for(chrono::milliseconds(1));
     
       return baton->result;
     
     finish /test/
